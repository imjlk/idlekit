{
  "name": "@idlekit/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "idk": "dist/main.js"
  },
  "scripts": {
    "dev": "bun src/main.ts",
    "dev:watch": "bunli dev --entry src/main.ts",
    "build": "bunli build --entry src/main.ts --runtime bun --outdir dist --sourcemap",
    "build:bin": "bunli build --entry src/main.ts --outdir dist --targets native",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src"
  },
  "dependencies": {
    "@bunli/core": "^0.8.0",
    "@idlekit/core": "workspace:*",
    "ajv": "^8.17.1",
    "bunli": "^0.7.1",
    "yaml": "^2.8.1",
    "zod": "^4.1.12"
  }
}
