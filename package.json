{
  "name": "idlekit",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/core build && bun run --cwd packages/cli build",
    "typecheck": "bun run --cwd packages/core typecheck && bun run --cwd packages/cli typecheck",
    "dev:cli": "bun run --cwd packages/cli dev",
    "test": "bun run --cwd packages/core test && bun run --cwd packages/cli test",
    "docs:verify": "bun tools/docs-verify.ts",
    "docs:verify:quick": "bun tools/docs-verify.ts --quick",
    "tune:regress": "bun tools/tune-regress.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9",
    "@types/node": "^24.3.0",
    "typescript": "^5.8.3"
  }
}
